# Development Dockerfile for OpenWebUI Frontend
# This Dockerfile runs SvelteKit dev server with HMR

FROM node:22-alpine3.20

# Set working directory
WORKDIR /app

# Install dependencies first (for better caching)
COPY package.json package-lock.json ./
RUN npm install --legacy-peer-deps

# Copy source code (will be overridden by volume mount in dev)
COPY . .

# Expose frontend port
EXPOSE 5175

# Set environment variables
ENV NODE_ENV=development
ENV PUBLIC_API_BASE_URL=http://localhost:8767

# Run SvelteKit dev server with HMR
# --host 0.0.0.0 allows access from outside container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5175"]
