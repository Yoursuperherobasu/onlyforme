# Development Dockerfile for LangBuilder Frontend
# This Dockerfile runs Vite dev server with HMR (Hot Module Replacement)

FROM node:20-alpine

# Set working directory
WORKDIR /app

# Install dependencies first (for better caching)
COPY package.json package-lock.json* ./
RUN npm install

# Copy source code (will be overridden by volume mount in dev)
COPY . .

# Expose frontend port
EXPOSE 3000

# Set environment variables
ENV NODE_ENV=development
ENV VITE_PORT=3000
ENV VITE_PROXY_TARGET=http://langbuilder-backend:8002

# Run Vite dev server with HMR
# Using dev:docker script which already has --host 0.0.0.0
CMD ["npm", "run", "dev:docker", "--", "--port", "3000"]
